import { TransformerPassDirective } from './transformer-pass.directive';

describe('TransformerPassDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformerPassDirective();
    expect(directive).toBeTruthy();
  });
});
